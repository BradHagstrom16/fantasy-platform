# Exact pins, not >= floors. A floor never pulls an environment forward, so
# each machine freezes at whatever pip resolved on its install date — that is
# how local and production silently diverged across 16 packages (see ADR-037).
# Upgrades here are deliberate: bump the pin, run the suite, smoke, deploy.
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.3.0
Flask-Limiter==4.1.1
Flask-Migrate==4.1.0
SQLAlchemy==2.0.51
Werkzeug==3.1.8
python-dotenv==1.2.2
email-validator==2.3.0
requests==2.34.2
gunicorn==26.0.0
psycopg2-binary==2.9.12
